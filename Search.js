class search {
    inputSearch = document.getElementById('pesquisar');
    async recebendoPesquisa(inputSearch) {
        const valorRecebido = inputSearch.value;

        try {
            const resposta = await fetch('https://localhost:3000/', {

            }
        }
    }
}